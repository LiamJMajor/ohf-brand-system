# Seed: Open Home Foundation

Shared-core content distilled from the OHF "Communications Strategy & Architecture" document (Google Doc, section 1.1 Shared Core), September 2026. `scaffold.py --seed open-home-foundation` copies these over the generic core templates and registers the projects in `projects.csv`.

Where the source doc is still "to write" (sections 1.2 and 1.3), these files carry TODOs. The comms doc remains the human-readable source; this seed is its agent-readable form. When one changes, change the other and log it in `decisions/`.
